# File Scanner

Program for working with daddy's files.

Functions:
- shows a list of files
- saves a lot of files
- searches for a file by name

Launching the program:

python src/program.py

After starting, you need to enter the path to the folder.